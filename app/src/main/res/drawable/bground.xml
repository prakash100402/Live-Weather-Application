<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Corner radius -->
    <corners android:radius="16dp" />

    <!-- Background color -->
    <solid android:color="#FFFFFF" />

    <!-- Optional: border or stroke -->
    <stroke
        android:width="2dp"
        android:color="#6619ED" />
</shape>
