<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Corner radius -->
    <corners android:radius="16dp" />

    <!-- Background color -->
    <solid android:color="#C7B2ED" />

    <!-- Optional: border or stroke -->
    <stroke
        android:width="3dp"
        android:color="#4A2885" />
</shape>